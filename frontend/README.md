# Iracus Logistic Frontend

React + TypeScript + Vite application for the Iracus Logistic MVP.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The app reads the backend URL from `VITE_API_URL`.
